# Launch the app (Mac)

# Activer l'environnement virtuel (une fois par terminal)
cd /Users/constellation/Documents/EEG_Beyond
source .venv/bin/activate  # ou créer: python -m venv .venv

# --- Lab Recorder (enregistrement XDF) ---
open "$(brew --prefix labrecorder)/LabRecorder.app"

# --- Perception (2 terminals) ---
cd /Users/constellation/Documents/EEG_Beyond && source .venv/bin/activate
cd data_collection/perception
pip install -r requirements.txt
python lsl_bridge.py
# Terminal 2:
cd /Users/constellation/Documents/EEG_Beyond && source .venv/bin/activate
cd data_collection/perception
python app.py
# Then open http://localhost:3000

# --- Imagery (1 terminal) ---
cd /Users/constellation/Documents/EEG_Beyond && source .venv/bin/activate
cd data_collection/imagery
pip install -r requirements.txt
python app.py
# Then open http://localhost:8080

# --- Neurofeedback (1 terminal) ---
cd /Users/constellation/Documents/EEG_Beyond && source .venv/bin/activate
cd data_collection/neurofeedback
pip install -r requirements.txt
python app.py
# Then open http://localhost:8090
